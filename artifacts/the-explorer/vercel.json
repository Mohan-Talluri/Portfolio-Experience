{
  "outputDirectory": "dist",
  "buildCommand": "cd ../.. && pnpm install --frozen-lockfile && pnpm --filter @workspace/the-explorer run build",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
